//  7. Define a function toUpperCaseWord that takes a string and returns it in uppercase.


function toUpperCaseWord(string){
    return string.toUpperCase();
}
console.log(toUpperCaseWord('vaishnavi'))